---
deployment:
  tasks:
    - export DEPLOYPATH=/home/laflot6/filemanagerapp
    - /bin/cp -r config db loaders mailing middleware models routes sequelize services third-party-api tmp app.js package.json $DEPLOYPATH